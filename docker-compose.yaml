version: '3.1'

services:

  db:
    ports:
    - "5432:5432"
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: test
    volumes:
    #mapping the db path to the host folder
    - ./db:/var/lib/postgresql/data